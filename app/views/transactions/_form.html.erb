<%= form_with(model: transaction, local: true) do |form| %>
  <% if transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
      <% transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="box box-primary container box-body">
  <div class="form-row">
  <div class="form-group col-md-12">
    <%= form.label :typepayment_id %>
    <%= form.collection_select :typepayment_id , Typepayment.where(user_id: current_user.id), :id, :nome, {}, class: "form-control" %>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :company_id %>
    <%= form.collection_select :company_id , Company.where(user_id: current_user.id), :id, :nome, {}, class: "form-control" %>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :companyprovider_id %>
    <%= form.collection_select :companyprovider_id , Companyprovider.where(user_id: current_user.id), :id, :nome, {}, class: "form-control" %>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :accountfinancial_id %>
    <%= form.collection_select :accountfinancial_id , Accountfinancial.where(user_id: current_user.id), :id, :nome, {}, class: "form-control" %>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :accountbank_id %>
    <%= form.collection_select :accountbank_id , Accountbank.where(user_id: current_user.id), :id, :nome, {}, class: "form-control" %>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :doctype_id %>
    <%= form.collection_select :doctype_id , Doctype.where(user_id: current_user.id), :id, :nome, {}, class: "form-control" %>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :category_id %>
    <%= form.collection_select :category_id , Category.where(user_id: current_user.id), :id, :nome, {}, class: "form-control" %>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :data_vencimento %>
    <%= form.date_field :data_vencimento , class: "form-control"%>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :data_baixa %>
    <%= form.date_field :data_baixa , class: "form-control"%>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :historico %>
    <%= form.text_area :historico , class: "form-control"%>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :valor_operacao %>
    <%= form.text_field :valor_operacao , class: "form-control"%>
  </div>

  <div class="fielform-group col-md-12">
    <%= form.label :valor_acrescimo %>
    <%= form.text_field :valor_acrescimo , class: "form-control"%>
  </div>

  <div class="form-group col-md-12">
    <%= form.label :valor_desconto %>
    <%= form.text_field :valor_desconto , class: "form-control"%>
  </div>


  <div class="form-group col-md-12">
    <%= form.label :Anexo %>
    <%= form.file_field :documents, multiple: true %>
  </div>

  <div class="box-footer text-center">
    <%= form.submit  "Cadastrar" , class:'btn btn-success'%>
  </div>
<% end %>
