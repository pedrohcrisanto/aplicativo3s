<% @transactions.each do |transaction| %>
      <%= transaction.company.codigo_contabil %><%= transaction.company.cnpj%><%= transaction.calcula_datas_max %><%= transaction.calcula_datas_min %>N0512340000017
    21<%= transaction.data_baixa %>
    32<%= transaction.typepayment.nome == 'Recebimento' ?(transaction.accountbank.codigo_contabil) : ('00000') %><%= transaction.typepayment.nome == 'Pagamento' ?(transaction.accountbank.codigo_contabil) : ('00000')%><%=transaction.calcula_valor_liquido.to_s.gsub('.', '') %>000<%=transaction.historico %><%=transaction.company.codigo_contabil %>
    33<%= transaction.typepayment.nome == 'Pagamento' ?(transaction.companyprovider.codigo_contabil_fornecedor) : ('00000') %><%= transaction.typepayment.nome == 'Recebimento' ?(transaction.companyprovider.codigo_contabil_fornecedor) : ('00000') %><%=transaction.valor_operacao.to_s.gsub('.', '') %>000<%=transaction.accountfinancial.historico %><%=transaction.company.codigo_contabil %>
    <% if transaction.valor_operacao != transaction.calcula_valor_liquido %>
    34<%= transaction.calcula_valor_liquido > (transaction.valor_operacao) ?(transaction.accountfinancial.codigo_contabil) : ('00000') %><%= transaction.calcula_valor_liquido < (transaction.valor_operacao) ?(transaction.accountfinancial.codigo_contabil) : ('00000') %><%= transaction.calcula_valor_liquido > (transaction.valor_operacao) ?(transaction.valor_acrescimo) : (transaction.valor_desconto) %>000<%= transaction.accountfinancial.historico%><%= transaction.company.codigo_contabil%>
    <% end %>
<% end %>
99
