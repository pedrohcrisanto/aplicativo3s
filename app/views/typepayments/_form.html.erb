<%= form_with(model: typepayment, local: true) do |form| %>
  <% if typepayment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(typepayment.errors.count, "error") %> prohibited this typepayment from being saved:</h2>

      <ul>
      <% typepayment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_area :descricao %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
