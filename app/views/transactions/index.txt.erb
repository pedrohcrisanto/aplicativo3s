<% @transactions.each do |transaction| %>
    <%= transaction.company.codigo_contabil.to_s.rjust(7, "0")%><%=transaction.company.cnpj.to_s.rjust(14, "0")%><%= transaction.calcula_datas_max %><%= transaction.calcula_datas_min %>N0512340000017
    21V<%= transaction.data_baixa %>
    32<%= transaction.typepayment.nome == 'Recebimento' ?(transaction.accountbank.codigo_contabil.to_s.rjust(7, "0")) : ('0000000') %><%= transaction.typepayment.nome == 'Pagamento' ?(transaction.accountbank.codigo_contabil).to_s.rjust(7, "0") : ('0000000')%><%=transaction.calcula_valor_liquido.to_s.gsub('.', '').to_i.to_s.rjust(15, "0") %>000<%=transaction.historico %><%=transaction.company.codigo_contabil %>
    33<%= transaction.typepayment.nome == 'Pagamento' ?(transaction.companyprovider.codigo_contabil_fornecedor) : ('00000') %><%= transaction.typepayment.nome == 'Recebimento' ?(transaction.companyprovider.codigo_contabil_fornecedor) : ('00000') %><%=transaction.valor_operacao.to_s.gsub('.', '').to_i.to_s.rjust(15, "0") %>0000000<%=transaction.accountfinancial.historico.to_s.rjust(512, " ") %><%=transaction.company.codigo_contabil.to_s.rjust(7, "0") %>
    <% if transaction.valor_operacao != transaction.calcula_valor_liquido %>
    34<%= transaction.calcula_valor_liquido > (transaction.valor_operacao) ?(transaction.accountfinancial.codigo_contabil.to_s.rjust(7, "0")) : ('0000000') %><%= transaction.calcula_valor_liquido < (transaction.valor_operacao) ?(transaction.accountfinancial.codigo_contabil.to_s.rjust(7, "0")) : ('0000000') %><%= transaction.calcula_valor_liquido > (transaction.valor_operacao) ?(transaction.valor_acrescimo.to_s.gsub('.', '').to_i.to_s.rjust(15, "0")) : (transaction.valor_desconto.to_s.gsub('.', '').to_i.to_s.rjust(15, "0")) %>0000000<%= transaction.accountfinancial.historico.to_s.rjust(512, " ")%><%= transaction.company.codigo_contabil.to_s.rjust(7, "0")%>
    <% end %>
<% end %>
    99
